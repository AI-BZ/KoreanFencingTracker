# app.fencingmind.ai 서비스 의존성
#
# 공유 의존성(fastapi, uvicorn, jinja2, supabase, shared_core 등)은
# 모노레포 루트에서 관리됩니다. 이 파일은 app 서비스 고유 의존성만 명시합니다.
#
# 설치 (Mac Studio ARM64):
#   arch -arm64 python3 -m pip install -r services/app/requirements.txt

# 웹 푸시 (Phase 4) — 표준 Web Push / FCM 발송에 사용.
# pywebpush가 없으면 dispatcher._send_web_push 는 graceful no-op로 동작합니다.
pywebpush>=1.14.0
